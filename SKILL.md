---
name: overseas-stock-tax
description: 中国境内自然人通过富途/长桥交易港股美股的财产转让所得税务申报 skill。生成符合北上广深杭税局要求的逐笔底稿、年度申报字段、四算法对比表与滞纳金测算。Use when 用户提到 "海外股票个税"、"港美股补税"、"富途/长桥 申报"、"境外所得 财产转让所得"、"CRS 补税"、"自查申报海外股票"。
---

# Overseas Stock Tax Filing Skill (CN Resident · 港美股 · 自然人)

## When to invoke

触发关键词（任一即可）：
- "海外股票"、"港美股"、"美股"、"港股" + "个税" / "补税" / "申报"
- "富途"、"Futu"、"Moomoo"、"长桥"、"Longbridge"、"Longport" + "税" / "申报"
- "境外所得"、"财产转让所得"、"B 表"
- "CRS 补税"、"自查 海外股票"、"被税局约谈 海外"

不适用场景：
- 企业主体的二级市场买卖（涉及增值税 6%+ 企业所得税 25%，规则不同 — 见 `references/01-政策依据.md` §2.4）
- 沪深港通直连账户（**暂免征至 2027-12-31**，与本 skill 不同）
- 员工股票期权 / RSU / 限售股（适用财税[2009]167 号、35 号文，不在本 skill 范围）

## Core principles (must read before any action)

1. **税目锁定**：本 skill 仅处理"财产转让所得" + "利息股息红利所得"，按 20% 比例税率（个税法第三条第三项）。
2. **金标准算法**：默认 **滚动加权平均法**（在多地税务师实务中被广泛使用、部分地方税务局已明确接受；参见 `references/07-税局认可的加权平均算法.md`）。
3. **辅助算法对比**：同时跑 **FIFO / 移动加权平均 / 个别计价**，输出四算法税基差异表。
4. **年度互抵 + 跨年禁止结转**：依据国税函[2006]1200 号；同年度盈亏可互抵，跨年亏损不能结转（见 `references/02-计算口径与四算法对比.md`）。
5. **跨国别不互抵**：港股 / 美股 / 其他市场分国别独立计算，分别填《境外所得抵免明细表》。
6. **汇率双轨输出**：法规口径（年末 12-31 中间价）+ 实操口径（买入日锁定原值），让用户与税局窗口沟通时灵活选择。
7. **不替用户做税务决定**：所有有争议的口径（互抵范围、合理税费边界、汇率轨道）必须以**明确告警 + 三个版本对比**呈现，让用户/会计签字选择。

## Workflow (must follow in order)

### Step 1 — 5 个运行时交互（不可跳过）

按顺序问用户，回答前不要进入下一步：

1. **申报年份**：要计算的纳税年度（例：2024，将按 2025 年 3-6 月汇算口径处理）。可输入多年份做批量计算。
2. **跨券商持仓转入转出**：富途↔长桥之间是否有同一只股票转账记录？若有，要求用户提供原券商成本结转单或人工录入原值（系统无法自动识别"转入"vs"买入"的区别）。
3. **是否包含股息红利**：从用户上传的资料中识别是否有分红记录；若有，自动按"利息股息红利所得"分流，按 20% 单独计算并算境外抵免。
4. **境外已扣税款抵免**：识别美股 W-8BEN 预扣 10%、港股股息预扣等；询问是否需要填《境外所得抵免明细表》（默认是）。
5. **居住城市**（北京/上海/广州/深圳/杭州/其他）：决定 references 里调用哪个地方口径模块。

### Step 2 — 解析券商流水

用户上传 Excel / PDF / CSV 后：
- 富途：调用 `scripts/parse_futu.py`（支持月结单 PDF + Excel 导出）
- 长桥：调用 `scripts/parse_longbridge.py`（PDF 密码 = 手机后4 + 证件后4，需问用户）
- 其他/通用：`scripts/parse_excel.py` 兜底（标准化为统一 schema）

统一 schema 字段：`trade_date, market, symbol, name, side, qty, price, amount_local, currency, commission, platform_fee, stamp_duty, settlement_fee, sec_fee, other_fee, account`

### Step 3 — 四算法并行计算成本基础

调用 `scripts/cost_basis.py`，对每只股票分别用：
- 滚动加权平均法（默认 / 金标准）
- FIFO
- 移动加权平均
- 个别计价（需用户标注，如未标注退化为 FIFO）

输出每笔卖出的"实际减持收益"4 个版本。

### Step 4 — 汇率换算（双轨）

调用 `scripts/fx_rate.py`：
- **轨道 A（法规口径）**：所有金额按申报年 12-31 人民币中间价折算
- **轨道 B（实操口径）**：买入金额按买入日上月末汇率锁定，卖出金额按申报年 12-31 汇率折算

中间价数据源：`assets/cny_mid_rate.json`（中国外汇交易中心历史数据，本地缓存）；缺失时调用 `fx_rate.py --refresh` 拉取。

### Step 5 — 应纳税额计算

调用 `scripts/compute_tax.py`：
- 按"年度 × 国别"维度分池
- 池内盈亏互抵后 × 20%（亏损池税额=0，**亏损不可跨年结转**）
- 股息部分单独计算 + 抵免限额（美股 10% 预扣可抵）
- 输出每年/每池的应纳税额

### Step 6 — 滞纳金（如适用）

调用 `scripts/penalty.py`：
- 各所得年度独立起算：起算日 = 次年 7-1
- 终止日 = 用户指定（默认今天）
- 日万分之五（年化 18.25%）
- **不要合并多年**计算

### Step 7 — 多 Agent 对账（输出前必做）

**任意一个对账失败都必须停止输出，向用户报告**。详见 §Hard constraints A.3。
按顺序跑 5 个对账节点，每个节点都启动独立 Agent（避免主上下文偏见）：
1. 数据完整性对账（原始 PDF/Excel ↔ 解析输出）
2. 算法一致性对账（四算法的不变量）
3. 年度勾稽对账（逐笔/月/季/年汇总自洽）
4. 金标准回归（合成基线 2024-02 = 15,000.00）
5. 最终复核（输出 Excel 字段与 B 表对齐）

调用 `scripts/reconcile.py` 跑 1-4，节点 5 由独立 Agent 直接打开 Excel 复核。

### Step 8 — 输出四份文件

通过对账后，调用 `scripts/render_report.py`，输出到用户指定目录：
1. `01_逐笔交易底稿_{年份}.xlsx` — 完整成交 + 4 算法成本追踪 + 汇率转换轨迹（税局可能调阅）
2. `02_B表填报字段_{年份}.xlsx` — 直接对应电子税务局《年度自行纳税申报表（B表）》+《境外所得抵免明细表》字段
3. `03_四算法对比表_{年份}.xlsx` — 四种算法税基差异 + 双汇率口径差异 + 选择建议
4. `04_税务综合报告与合规建议_{年份}.xlsx` — **必须输出**：税务摘要 + 合法节税建议 + 风险提示 + 强制合规声明
   - 合规声明必须包含 3 条核心内容（以醒目方式呈现）：
     1. **依法纳税是中国公民的基本义务**（《宪法》第 56 条）
     2. **本报告仅供参考，最终以主管税务机关认定为准**
     3. **建议委托具有证书的税务师签字背书**，skill 不承担税务法律责任
   - 节税建议范围：**仅限合法范畴**（年度内盈亏互抵优化、合理税费充分扣除、税收协定抵免最大化、申报时点选择、长期持仓 vs 短期高频的税负差异）
   - **禁止**给出激进/灰色避税建议（如：故意推迟申报、隐匿境外账户、用他人身份持仓、虚构亏损等）— 检测到此类倾向必须拒绝并报警

### Step 8 — 必读告警（输出末尾）

强制提醒用户：
- 滞纳金年化 18.25%，多年累积可能超本金
- 亏损年度仍要补股息税
- 跨年不可结转亏损 — 多年合算可能比逐年算贵
- 算法选择要与主管税务机关确认，非全国统一口径
- 北京/上海/深圳执行最严，杭州/广州相对温和（**不代表免查**）
- 富途/长桥香港主体已在 CRS 覆盖范围（参见 `references/06-五地税局口径与补税案例.md`）

## Repository layout

```
overseas-stock-tax/
├── SKILL.md
├── references/
│   ├── 01-政策依据.md                      # 税目、税率、法规文号
│   ├── 02-计算口径与四算法对比.md           # 互抵规则、四算法实现细节
│   ├── 03-富途流水字段与导出.md             # 富途月结单/OpenAPI/Moomoo 字段
│   ├── 04-长桥流水字段与导出.md             # 长桥月结单密码、字段
│   ├── 05-汇率换算双轨规则.md               # 法规 vs 实操、数据源
│   ├── 06-五地税局口径与补税案例.md         # 北上广深杭执行差异 + 真实案例
│   ├── 07-税局认可的加权平均算法.md         # 案例校验版金标准（项目内部经验）
│   └── 08-滞纳金与境外抵免计算.md           # 滞纳金 + 中美/中港税收协定
├── scripts/
│   ├── parse_futu.py
│   ├── parse_longbridge.py
│   ├── parse_excel.py
│   ├── cost_basis.py
│   ├── fx_rate.py
│   ├── compute_tax.py
│   ├── penalty.py
│   └── render_report.py
├── templates/
│   └── （由 render_report.py 生成 Excel，模板内嵌在脚本中）
└── assets/
    └── cny_mid_rate.json                  # 中国外汇交易中心历史中间价缓存
```

## Hard constraints (DO NOT violate)

### A. 数据与计算的强制规则（**用户明确要求，不可降级**）

1. **PDF 解析必须用脚本，禁止视觉识别**
   - 富途 / 长桥 月结单 PDF → 必须调用 `scripts/parse_futu.py` / `parse_longbridge.py`（基于 `pdfplumber` 或 `pypdf` 文本层提取）
   - **禁止**用 Read 工具的图片视觉能力直接看 PDF 数字，**禁止**让 LLM 从截图读数字
   - 若 PDF 是扫描件（无文本层），必须先用 OCR 转文本（如 `tesseract`），并在 SKILL 输出中告警"原始 PDF 为扫描件，OCR 可能引入误差，请人工核对"
   - 任何"我看到 PDF 上写的是 X"的判断都必须有脚本输出对照，否则视为违规

2. **核心计算必须用脚本，禁止 LLM 心算**
   - 成本基础、应纳税所得额、应纳税额、滞纳金、汇率换算、境外抵免限额 — 全部走 `scripts/` 下的 Python 脚本
   - **禁止** LLM 在对话中口算 / 估算任何金额；如果脚本未实现，先补脚本再算
   - 所有金额使用 Python `Decimal`（禁用 float），保留 4 位小数；输出时按显示规则四舍五入
   - 每一步计算必须能复现：脚本须输出"输入摘要 + 公式 + 中间值 + 结果"四段日志

3. **重要计算节点必须做多 agent 对账**（在输出最终文件前）
   - **节点 1（数据完整性）**：解析完所有流水后，启动一个独立 Agent（subagent_type=Explore），让它从原始 PDF / Excel 重新统计"买入笔数、卖出笔数、买入总金额、卖出总金额、唯一股票数"，与 `parse_*.py` 的输出对账。差异 > 0.01 即阻塞输出
   - **节点 2（算法一致性）**：四种成本算法跑完后，启动独立 Agent 验证"四算法的总买入成本相同 + 期末持仓相同"（应纳税所得额可不同）。不通过 = 阻塞
   - **节点 3（年度勾稽）**：年度盈亏汇总后，启动独立 Agent 验证"sum(逐笔实际减持收益) == 月度汇总 == 季度汇总 == 年度汇总"，公式自洽性检查。不通过 = 阻塞
   - **节点 4（金标准回归）**：用合成基线数据跑一遍当前算法，验证 2024-02 月度收益 = 15,000.00（误差 ≤ 0.01）。不通过 = 阻塞
   - **节点 5（最终复核）**：所有 Excel 文件生成后，启动独立 Agent 打开三份输出文件，复核"申报字段与 B 表名称一一对应、滞纳金独立计算未合并、汇率双轨齐全、四算法对比表数字与底稿一致"。不通过 = 阻塞输出

   阻塞机制：任一对账失败，立即停止输出三份文件，向用户报告差异 + 触发原因 + 建议排查方向。

### B. 业务规则约束

- ❌ 不要给税务**结论**性建议（"必须交 X"）— 只输出"按某口径计算结果是 X"
- ❌ 不要替用户选择算法 — 输出四个版本让用户决策
- ❌ 不要假设跨券商持仓自动归并 — 必须问用户
- ❌ 不要把企业算法（增值税 6% + 所得税 25%）混入自然人计算
- ❌ 不要把港股通暂免政策套用到富途/长桥（这是不同通道）
- ❌ **绝不**给激进 / 灰色 / 违法避税建议（隐匿境外账户、虚构亏损、用他人身份持仓、故意推迟申报、伪造完税凭证等）
- ✅ 始终输出 references 里的政策文号和链接，让结果可追溯
- ✅ 滞纳金、抵免限额、汇率换算 — 每一步都打出"依据 + 数字"日志
- ✅ **每次会话结束的最后一条消息必须包含**：「本 skill 输出仅供参考，实际申报金额以主管税务机关认定为准；依法纳税是中国公民的基本义务（《宪法》第 56 条）；建议在最终申报前委托执业税务师复核签字。」

## Reference reading order

新会话第一次用本 skill 时按顺序读：
1. `references/01-政策依据.md`（5 分钟）
2. `references/02-计算口径与四算法对比.md`（10 分钟，**最重要**）
3. `references/07-税局认可的加权平均算法.md`（金标准）
4. 其他按需查阅
